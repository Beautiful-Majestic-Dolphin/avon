apiVersion: v1
kind: Service
metadata:
  name: {{ include "avon.adminApi.fullname" . }}
  labels:
    {{- include "avon.adminApi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.adminApi.service.type }}
  ports:
    - port: {{ .Values.adminApi.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 9090
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "avon.adminApi.selectorLabels" . | nindent 4 }}
