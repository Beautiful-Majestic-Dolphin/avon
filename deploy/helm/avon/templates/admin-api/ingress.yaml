{{- if and .Values.adminApi.ingress.enabled (not (and .Values.serviceMesh.enabled (eq .Values.serviceMesh.provider "istio"))) -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "avon.adminApi.fullname" . }}
  labels:
    {{- include "avon.adminApi.labels" . | nindent 4 }}
  {{- with .Values.adminApi.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.adminApi.ingress.className }}
  ingressClassName: {{ .Values.adminApi.ingress.className }}
  {{- end }}
  {{- if .Values.adminApi.ingress.tls }}
  tls:
    {{- range .Values.adminApi.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.adminApi.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "avon.adminApi.fullname" $ }}
                port:
                  name: http
          {{- end }}
    {{- end }}
{{- end }}
