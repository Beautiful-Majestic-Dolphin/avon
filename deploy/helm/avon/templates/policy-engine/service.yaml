apiVersion: v1
kind: Service
metadata:
  name: {{ include "avon.policyEngine.fullname" . }}
  labels:
    {{- include "avon.policyEngine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.policyEngine.service.type }}
  ports:
    - port: {{ .Values.policyEngine.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "avon.policyEngine.selectorLabels" . | nindent 4 }}
