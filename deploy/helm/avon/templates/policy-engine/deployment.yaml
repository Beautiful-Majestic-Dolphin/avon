apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "avon.policyEngine.fullname" . }}
  labels:
    {{- include "avon.policyEngine.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.policyEngine.replicaCount }}
  selector:
    matchLabels:
      {{- include "avon.policyEngine.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
        checksum/config: {{ include (print $.Template.BasePath "/policy-engine/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/common/secrets.yaml") . | sha256sum }}
        {{- include "avon.meshAnnotations" . | nindent 8 }}
        {{- with .Values.policyEngine.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "avon.policyEngine.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "avon.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.policyEngine.podSecurityContext | nindent 8 }}
      containers:
        - name: policy-engine
          securityContext:
            {{- toYaml .Values.policyEngine.securityContext | nindent 12 }}
          image: {{ include "avon.image" (dict "global" .Values.global "image" .Values.policyEngine.image) }}
          imagePullPolicy: {{ .Values.policyEngine.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.policyEngine.service.port }}
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          env:
            - name: AVON_LOG_LEVEL
              value: {{ .Values.policyEngine.logLevel | default "info" | quote }}
            - name: AVON_HTTP_PORT
              value: {{ .Values.policyEngine.service.port | quote }}
            - name: AVON_METRICS_PORT
              value: "9090"
          envFrom:
            - configMapRef:
                name: {{ include "avon.policyEngine.fullname" . }}-config
            - secretRef:
                name: {{ include "avon.fullname" . }}-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            {{- toYaml .Values.policyEngine.resources | nindent 12 }}
      {{- with .Values.policyEngine.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.policyEngine.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if not .Values.policyEngine.affinity }}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    {{- include "avon.policyEngine.selectorLabels" . | nindent 20 }}
                topologyKey: kubernetes.io/hostname
      {{- end }}
      {{- with .Values.policyEngine.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
