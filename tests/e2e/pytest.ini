[pytest]
# AVON E2E Test Configuration

# Test discovery
testpaths = scenarios
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Async configuration
asyncio_mode = auto

# Timeout for tests (seconds)
timeout = 120
timeout_method = thread

# Output configuration
addopts =
    -v
    --tb=short
    --strict-markers
    -ra

# Markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    multi_peer: marks tests that require 3+ agents
    missing(reason): scenario has no executable body; reported as MISSING, never run
    l0: build layer
    l1: crypto layer
    l2: control plane layer
    l3: data plane layer
    l4: policy layer
    l5: device trust layer
    lnet: network conditions layer

# Logging
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(name)s: %(message)s
log_cli_date_format = %H:%M:%S

# Warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
